meta {
  name: getBlockProduction
  type: http
}

post {
  url: https://solana-mainnet.g.alchemy.com/v2/{{api_key}}
  body: json
  auth: inherit
}

body:json {
  {
    "jsonrpc": "2.0",
    "method": "getBlockProduction",
    "params": [],
    "id": "bru"
  }
}

settings {
  encodeUrl: true
}

docs {
  Returns recent block production information from the current or previous epoch.
  
  **Args**:
  - Configuration (optional): Optional configuration object containing additional settings
  
  **Returns**: Block production information for the specified range
}

