meta {
  name: getblockcount
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}
  body: json
  auth: basic
}

auth:basic {
  username: {{rpcUser}}
  password: {{rpcPassword}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "jsonrpc": "1.0",
    "id": "bru",
    "method": "getblockcount",
    "params": []
  }
}

docs {
  Returns the height of the most-work fully-validated chain.
  
  The genesis block has height 0. This represents the current blockchain height,
  which is the number of blocks in the longest valid chain.
}
